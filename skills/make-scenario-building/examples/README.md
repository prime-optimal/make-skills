# Make scenario-building examples

These examples are copied from this repository’s canonical `blueprints/` directory and renamed to stable teaching filenames.

## Provenance

| Example file | Source file | Why selected |
|---|---|---|
| `example-webhook-router.json` | `blueprints/scenario-orchestrator.json` | Representative webhook entrypoint with router-based orchestration and downstream branching. |
| `example-subscenario-orchestration.json` | `blueprints/scenario-adriana-agent.json` | Representative subscenario-oriented orchestration with nested tool/subscenario behavior. |
| `example-ai-enrichment.json` | `blueprints/scenario-2a-extract-info.json` | Representative AI enrichment/extraction flow used as reusable subscenario logic. |

## Notes

- These are reference exemplars for skill guidance.
- Keep source-of-truth edits in `blueprints/`; refresh these examples by recopying from the source files when needed.
